# Tracing

这个仓库目录包含追踪法五个实验，每个实验都是一个独立的小工程：

- `1.0`：速度比对导引弹道的影响
- `2.0`：初始目标视线角对导引弹道的影响
- `3.0`：初始弹目距离对导引弹道的影响
- `4.0`：允许攻击区的确定
- `5.0`：考虑姿态控制回路的追踪法仿真

每个子目录都自带 `config.py`、`main.py`、`README.md` 和 `requirements.txt`，可以独立运行。
